# 使用
```shell script
// 初始化模板
tmc init --template=模板名 --templateType=模板类型 --projectName=生成的项目名 --projectDesc=项目描述
// 显示所有模板
tmc list
// 新增模板
tmc add --name --description --npm=模板的npm包名称
// 删除模板
tmc delete --name
```
